Django>=5.2.8,<6.0
djangorestframework>=3.15
django-environ>=0.11
paho-mqtt>=2.1
websocket-client>=1.8
psycopg[binary]>=3.2
gunicorn>=23.0
whitenoise>=6.7
cryptography>=42
celery>=5.4
redis>=5.0
xhtml2pdf>=0.2.16
pysnmp>=7.1
django-axes>=8.3
django-otp>=1.5
qrcode[pil]>=8.0
openpyxl>=3.1
# Cliente oficial de la API de Claude, para el diagnóstico automático de alertas
# críticas (apps/monitoreo/diagnostico_ia.py). A diferencia de los webhooks de Teams y
# Telegram —que son un POST y se resuelven con urllib— acá la dependencia sí se
# justifica: reintentos, streaming y tipos de la API. Sin ANTHROPIC_API_KEY no se usa.
anthropic>=0.40
